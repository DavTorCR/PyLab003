Actually look at the lab_chat to see code you didn't 
create and see if you can understand the parts of a
function, what is the function, parameters, when does 
it start and end.

3 assigned.

what is the function:
Defined get underscore peer underscore
node open parenthetical username close 
parenthetical colon

the parameters in this function:
It has one parameter,
open parenthetical username close 
parenthetical colon

Which line does it start:
line nine.

Which line does it end:
line fourteen




what is the function:
defined join underscore group

the parameter in this function:
It has two functions, node and group
Which line does it start:
16
Which line does it end:
18















what is the function:
defined get underscore channel
the parameter in this function:
it uses two, node and group
Which line does it start:
60
Which line does it end:
62
















what is the function:
defined chat underscore task
the parameter in this function:
it uses four,
ctx, pipe, n, group
Which line does it start:
20
Which line does it end:
58

